[build]
builder = "nixpacks"
buildCommand = "npm install --prefix server && npm install --prefix client && npx --prefix client vite build"

[deploy]
startCommand = "node server/src/index.js"
restartPolicyType = "on_failure"

[nixpacks]
nodejs = "20"
